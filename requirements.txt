fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-dotenv==1.0.1
httpx>=0.28.1

# ── Cloud SDKs (only needed for live mode; mock mode works without them) ──
boto3==1.34.101                # AWS Bedrock
google-cloud-aiplatform==1.133.0  # GCP Vertex AI
openai==1.30.1                 # Azure OpenAI (uses OpenAI Python SDK)
